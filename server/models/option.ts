// class for Option with text, isCorrect    
export class Option {
    text: string;
    isCorrect: boolean;

    constructor(text: string, isCorrect: boolean) {
        this.text = text;
        this.isCorrect = isCorrect;
    }
}

